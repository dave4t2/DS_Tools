# Generated by GitHub Copilot
test_case: PAM-SMOKE-001
payload_ref: FISTesting/payloads/pain001_base.json

# Preconditions: base template already satisfies all requirements —
# EUR, amount 1000.00, single_cover routing, no magic debtorAccount
# (fault_injection = None, every downstream leg follows its deterministic OK callback).
# No overrides or omit clauses are needed.

steps:

  # Step 1 — POST a well-formed payment; assert HTTP 202 + summary with id/reference
  - action: inject_payment
    params:
      payload_ref: FISTesting/payloads/pain001_base.json
    assert:
      keyword: assert_http_status
      params:
        expected: 202
    verifies: [shared.R1]

  # Step 2 — Wait for PENDING_SUBMISSION; verify pre-submit status on the JSON API
  - action: wait_for_payment_status
    params:
      allowed: [PENDING_SUBMISSION]
      timeout: 30.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 3 — Verify pre-submit status on the fis.business.events Kafka topic
  - action: search_payment_kafka
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 4 — Verify pre-submit status on the operator UI detail page
  - action: search_payment_ui
    assert:
      keyword: assert_ui_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 5 — Operator releases the payment; assert submit-to-csm accepts with 202
  - action: submit_to_csm
    assert:
      keyword: assert_http_status
      params:
        expected: 202
    verifies: [shared.R3]

  # Step 6 — Verify payment transitions out of PENDING_SUBMISSION into the post-submit pipeline
  # allowed set matches shared.R4 exactly: {PACS008_EMITTED, POSTED, ACKNOWLEDGED, SETTLED, REPAIR}
  - action: wait_for_payment_status
    params:
      allowed: [PACS008_EMITTED, POSTED, ACKNOWLEDGED, SETTLED, REPAIR]
      timeout: 30.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [PACS008_EMITTED, POSTED, ACKNOWLEDGED, SETTLED, REPAIR]
    verifies: [shared.R4]

  # Step 7 — Poll JSON API until SETTLED within 60 s (shared.R5 timing bound + shared.R6 API channel)
  - action: wait_for_payment_status
    params:
      allowed: [SETTLED]
      timeout: 60.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R5, shared.R6]

  # Step 8 — Confirm settlement business event published on fis.business.events Kafka topic (shared.R5)
  - action: search_payment_kafka
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R5]

  # Step 9 — Confirm SETTLED status reported on the operator UI detail page (shared.R7)
  - action: search_payment_ui
    assert:
      keyword: assert_ui_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R7]
