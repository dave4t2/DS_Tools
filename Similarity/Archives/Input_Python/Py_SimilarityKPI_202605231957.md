# Python Similarity KPI Report

- **Script**: `py_similarity_kpi.py`
- **Executed**: 202605231957
- **Input directory**: `G:\My Drive\0 - Dev\0 - Workspaces\SG_FISTF\FISTF\Tools\Similarity\Python`
- **Reference file**: `PAM-USD-MULTICOVER-001 v2.py`  *(oldest by creation date)*
  - Created: 2026-05-23 16:53 | Lines: 394 | AST nodes: 2072
- **Threshold**: 0.9

## Results

| # | File | Created | Lines (Δ) | Nodes (Δ) | Edit blocks | Score | Status |
|---|------|---------|----------:|----------:|:-----------:|------:|--------|
| 1 | `PAM-USD-MULTICOVER-001 v1.py` | 2026-05-23 16:53 | 394 | 2072 | — | 1.0000 | PASS |
| 2 | `PAM-USD-MULTICOVER-001 v2 copy.py` | 2026-05-23 19:20 | 392 (-2) | 2058 (-14) | 13 | 0.8929 | FAIL |

## Summary

**Robustness score: 1/2 candidates above threshold (50.0%)**

## Diff details

### `PAM-USD-MULTICOVER-001 v2 copy.py`

```diff
--- PAM-USD-MULTICOVER-001 v2.py
+++ PAM-USD-MULTICOVER-001 v2 copy.py
@@ -5,7 +5,6 @@
 # Generated by GitHub Copilot
 import json
 import re
-import time
 import uuid
 from pathlib import Path
 
@@ -15,7 +14,7 @@
 
 from runner.context import context, reset_context, get as ctx_get, set as ctx_set
 
-base_url = "http://127.0.0.1:8080"  # mock runtime
+base_url = "http://127.0.0.1:8181"  # mock runtime
 
 
 def test_pam_usd_multicover_001(page, subtests):
@@ -27,8 +26,8 @@
         params = {
             "payload_ref": "FISTesting/payloads/pain001_base.json",
             "overrides": {
-                "creditTransferTransaction.currency": "USD",
-                "creditTransferTransaction.amount": "5000.00",
+                "creditTransferTransaction.currency": "EUR",
+                "creditTransferTransaction.amount": "6000.00",
                 "controlSum": "5000.00",
                 "cashfitMetadata.routingPattern": "multiple_cover",
             },
@@ -59,7 +58,7 @@
             keys = dotted_path.split(".")
             cur = obj
             for k in keys[:-1]:
-                if not isinstance(cur.get(k), dict):
+                if  isinstance(cur.get(k), dict):
                     return
                 cur = cur[k]
             cur.pop(keys[-1], None)
@@ -79,7 +78,7 @@
         for path in (params.get("omit") or []):
             _del_path(payload, path)
 
-        resp = httpx.post(f"{base_url}/payments", json=payload, timeout=5.0)
+        resp = httpx.post(f"{base_url}/tests", json=payload, timeout=5.0)
         ctx_set("last_response", resp)
         ctx_set("message_id", payload.get("messageId"))
         if 200 <= resp.status_code < 300:
@@ -102,7 +101,7 @@
     # ── Step 2 — wait_for_payment_status + assert_payment_status_in  (verifies: [shared.R2]) ──
     with subtests.test(msg="Step 2 — wait_for_payment_status + assert_payment_status_in (verifies shared.R2)"):
         params = {
-            "allowed": ["PENDING_SUBMISSION"],
+            "allowed": ["PENDING_"],
             "timeout": 30.0,
         }
 
@@ -139,7 +138,7 @@
         assertion_target = matched
 
... (14 more lines truncated)
```
