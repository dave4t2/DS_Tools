# Generated by GitHub Copilot — Pass 1
# Source: FISTesting/dsl/examples/PAM-USD-MULTICOVER-001/requirements.md
# Inherits: shared.R1..shared.R7  |  No local R-ids
test_case: PAM-USD-MULTICOVER-001
payload_ref: FISTesting/payloads/pain001_base.json

steps:

  # Step 1 — Submit the USD / multiple_cover variant; verify 202 + summary
  # Preconditions applied via overrides/omit:
  #   - currency USD (replaces EUR)
  #   - amount 5000.00 (replaces 1000.00); controlSum kept consistent
  #   - routingPattern multiple_cover (replaces single_cover)
  #   - remittanceInformation omitted
  #   - categoryPurpose omitted
  - action: inject_payment
    params:
      payload_ref: FISTesting/payloads/pain001_base.json
      overrides:
        creditTransferTransaction.currency: USD
        creditTransferTransaction.amount: "5000.00"
        controlSum: "5000.00"
        cashfitMetadata.routingPattern: multiple_cover
      omit:
        - creditTransferTransaction.remittanceInformation
        - paymentInformation.categoryPurpose
    assert:
      keyword: assert_http_status
      params:
        expected: 202
    verifies: [shared.R1]

  # Step 2 — Wait for PENDING_SUBMISSION; verify pre-submit status on API
  - action: wait_for_payment_status
    params:
      allowed: [PENDING_SUBMISSION]
      timeout: 30.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 3 — Verify pre-submit status on the business-events Kafka topic
  - action: search_payment_kafka
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 4 — Verify pre-submit status on the operator UI
  - action: search_payment_ui
    assert:
      keyword: assert_ui_payment_status_in
      params:
        allowed: [RECEIVED, VALIDATED, ENRICHED, PENDING_SUBMISSION]
    verifies: [shared.R2]

  # Step 5 — Operator releases payment; verify submit-to-csm accepts with 202
  - action: submit_to_csm
    assert:
      keyword: assert_http_status
      params:
        expected: 202
    verifies: [shared.R3]

  # Step 6 — Verify payment transitions out of PENDING_SUBMISSION
  - action: wait_for_payment_status
    params:
      allowed: [PACS008_EMITTED, POSTED, ACKNOWLEDGED, SETTLED]
      timeout: 30.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [PACS008_EMITTED, POSTED, ACKNOWLEDGED, SETTLED]
    verifies: [shared.R4]

  # Step 7 — Wait for SETTLED on API within 60 s; covers timing bound + API channel
  - action: wait_for_payment_status
    params:
      allowed: [SETTLED]
      timeout: 60.0
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R5, shared.R6]

  # Step 8 — Verify settlement event published to the business-events Kafka topic
  - action: search_payment_kafka
    assert:
      keyword: assert_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R5]

  # Step 9 — Verify SETTLED status reported on the operator UI
  - action: search_payment_ui
    assert:
      keyword: assert_ui_payment_status_in
      params:
        allowed: [SETTLED]
    verifies: [shared.R7]
